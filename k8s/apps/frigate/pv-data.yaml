apiVersion: v1
kind: PersistentVolume
metadata:
  name: frigate-data-pv
spec:
  capacity:
    storage: 10Gi
  accessModes:
  - ReadWriteOnce
  storageClassName: manual
  # FIXME: Make this more rubust
  hostPath:
    path: /data/frigate/media
