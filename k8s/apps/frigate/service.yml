apiVersion: v1
kind: Service
metadata:
  labels:
    app: frigate
  name: frigate
  namespace: applications
spec:
  ports:
  - port: 5000
    protocol: TCP
    targetPort: 5000
    nodePort: 30432
  selector:
    app: frigate
  type: NodePort
status:
  loadBalancer: {}
